#include<stdio.h>
#include<conio.h>
void main()
{
	int a;
	printf("ENTER THE no=");
	scanf("%d",&a);
		if((a*a)%10==a)
		{
		printf("%d is a autopollymorphic number",a);
		}
	else
	{
	printf("%d is not a autopollymorphic number",a);
}
getch();
}

